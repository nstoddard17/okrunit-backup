name: Backup Mirror

on:
  push:
    branches: [main]

jobs:
  mirror:
    name: Mirror to backup repo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to backup repo
        env:
          BACKUP_TOKEN: ${{ secrets.BACKUP_REPO_TOKEN }}
        run: |
          if [ -z "$BACKUP_TOKEN" ]; then
            echo "::warning::BACKUP_REPO_TOKEN secret not set — skipping mirror. Create a PAT with repo scope and add it as BACKUP_REPO_TOKEN secret."
            exit 0
          fi
          git remote add backup https://x-access-token:${BACKUP_TOKEN}@github.com/nstoddard17/okrunit-backup.git
          git push backup main || echo "::warning::Backup push failed — check BACKUP_REPO_TOKEN permissions"
