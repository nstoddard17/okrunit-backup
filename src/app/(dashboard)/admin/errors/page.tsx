import { ErrorMonitorTab } from "@/components/admin/errors/error-monitor-tab";

export const metadata = {
  title: "Error Monitor - Admin - OKrunit",
};

export default function AdminErrorsPage() {
  return <ErrorMonitorTab />;
}
